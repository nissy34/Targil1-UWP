<Page
    x:Class="GUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="24*"/>
            <RowDefinition Height="101*"/>
        </Grid.RowDefinitions>

        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
              <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!--<TextBlock  Text="TextBlock" Padding="10" FontSize="10" Margin="10,20" Style="{StaticResource BaseTextBlockStyle}" TextWrapping="Wrap" TextAlignment="Center"/>-->
            <TextBox  Text="TextBox" Padding="10" FontSize="12" Margin="10,20"  Grid.Column="0"  FontWeight="Bold" Style="{StaticResource AutoSuggestBoxTextBoxStyle}" />
            <Button Content="Button" Grid.Column="1"  Margin="10,20" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        </Grid>
        <ListView  Grid.Row="1" />

    </Grid>
</Page>
